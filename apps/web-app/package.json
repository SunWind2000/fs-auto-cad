{
  "$schema": "../../package-schema.json",
  "name": "fs-web-app",
  "version": "0.0.0",
  "type": "module",
  "description": "Main web application for the WebGL example",
  "scripts": {
    "dev": "vite",
    "build": "vite build"
  },
  "devDependencies": {
    "@fs/shared": "workspace:*",
    "@fs/core": "workspace:*"
  }
}